[build]
builder = "NIXPACKS"

[deploy]
# Inicializa el volumen de datos en el primer deploy, luego arranca Next.js
startCommand = "node scripts/init-volume.js && npm start"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
